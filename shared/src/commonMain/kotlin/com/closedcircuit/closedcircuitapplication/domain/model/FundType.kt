package com.closedcircuit.closedcircuitapplication.domain.model

enum class FundType(val displayText: String) {
    LOAN("Loans"), DONATION("Donations (Cash gifts)"), BOTH("Both (Loans and Donations)")
}